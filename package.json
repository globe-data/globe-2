{
  "name": "globe-2",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "dev": "concurrently \"npm run watch\" \"python -m uvicorn app.main:app --reload\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^8.0.1",
    "openapi-typescript-codegen": "^0.29.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@types/pako": "^2.0.3",
    "pako": "^2.1.0",
    "zod": "^3.23.8"
  }
}
